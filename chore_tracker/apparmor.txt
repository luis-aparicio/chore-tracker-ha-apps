#include <tunables/global>

profile chore_tracker flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/openssl>
  #include <abstractions/nameservice>

  # Capabilities
  network,
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # Node runtime (node:*-slim layout) and privilege drop (setpriv)
  /usr/local/bin/node ix,
  /usr/local/bin/** ix,
  /usr/bin/** ix,
  /bin/** ix,
  /sbin/setpriv ix,
  /usr/bin/setpriv ix,
  /lib/** mr,
  /lib64/** mr,
  /usr/lib/** mr,
  /usr/local/lib/** mr,

  # Entrypoint + application tree
  /entrypoint.sh rix,
  /app/** r,
  /app/node_modules/** mr,

  # Host identity / DNS (Node + glibc lookups)
  /etc/passwd r,
  /etc/group r,
  /etc/hosts r,
  /etc/hostname r,
  /etc/resolv.conf r,
  /etc/nsswitch.conf r,
  /etc/ssl/** r,
  /etc/localtime r,
  /usr/share/zoneinfo/** r,

  # Supervisor options and persistent state
  /data/** rw,

  # Node / V8 scratch, entropy, and typical self/cgroup probes
  /tmp/** rwk,
  /dev/urandom r,
  /dev/null rw,
  /dev/zero r,
  /proc/self/** r,
  /proc/sys/vm/overcommit_memory r,
  /proc/sys/kernel/random/uuid r,
  /sys/devices/system/cpu/** r,
  /sys/fs/cgroup/** r,
}
