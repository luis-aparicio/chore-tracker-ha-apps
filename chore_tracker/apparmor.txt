#include <tunables/global>

profile chore_tracker flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/openssl>

  # Capabilities
  network,
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # Node runtime (node:*-slim layout)
  /usr/local/bin/node ix,
  /usr/local/bin/** ix,
  /usr/bin/** ix,
  /bin/** ix,
  /lib/** mr,
  /lib64/** mr,
  /usr/lib/** mr,
  /usr/local/lib/** mr,

  # Application tree
  /app/** r,
  /app/node_modules/** mr,

  # Supervisor options and persistent state
  /data/** rw,

  # Node / V8 scratch and entropy
  /tmp/** rwk,
  /dev/urandom r,
  /dev/null rw,
  /proc/sys/vm/overcommit_memory r,
  /sys/devices/system/cpu/** r,
}
